# OSS Agent 阅读网站 — 产品需求文档

状态：待评审草案。本文定义产品需求，尚未确定技术栈。

## 1. 产品目标

建设一个围绕开源软件与编程 Agent 的精选阅读网站，涵盖 Kimi Code、Codex 等主题。站长编写 Markdown 文章，离线 Agent 为文章打标签，读者通过标题搜索、标签筛选和推荐阅读发现内容。

网站支持免费与付费阅读，通过服务端权限校验保护内容，并采取措施降低自动化抓取的规模。

## 2. 当前假设

- “文章”指站长编写的 Markdown 文件，不自动复制第三方内容；文章中可以附上外部来源链接。
- “离线 Agent”指不在读者请求链路中执行的后台任务，可以调用模型服务。除非后续明确要求，否则不限定为完全本地、无网络推理。
- “Key 方式”暂理解为通过兑换码开通阅读权限，不是收集读者的模型服务 API Key。
- 首版只提供一种有期限的全站阅读通行证。支付购买与兑换码开通获得相同权益，暂不支持单篇购买与自动续费订阅。
- 文章可以使用中文或英文。界面首版选择一种语言，文章元数据与搜索同时支持中英文。

基于这些假设推进实现，暂不扩展为交易市场、API 产品或通用内容发布平台。

## 3. 用户与权限

| 角色 | 能力 |
| --- | --- |
| 访客 | 浏览已发布文章的元数据、搜索标题与标签、阅读免费文章与付费文章预览 |
| 已登录读者 | 拥有访客能力，另可购买权限、兑换兑换码、查看权限到期时间 |
| 权限有效的读者 | 完整阅读已发布的付费文章 |
| 站长／管理员 | 编写与发布文件、运行标签任务、审核标签、管理兑换码与阅读权限 |

付费权限不包含草稿访问权限。管理操作需要单独的管理员授权。

## 4. Markdown 内容发布

### 4.1 源文件格式

文章存放于纳入版本控制的 `content/articles/` 目录。每个文件由 YAML front matter（头部元数据）和 Markdown 正文组成。Markdown 是编辑内容的唯一权威来源，自动生成的标签与搜索数据独立存储。

```md
---
slug: kimi-code-getting-started
title: Kimi Code 入门
summary: 安装配置说明与第一个实用工作流。
topics: [kimi-code]
tags: [setup, coding-agent]
language: zh
status: draft
access: free
preview: standard
published_at: null
updated_at: 2026-09-15
recommended: false
related: []
---

# Kimi Code 入门

这里填写文章正文。
```

### 4.2 编辑与发布规则

- 必填字段：唯一且稳定的 `slug`、`title`、`summary`、`topics`、`language`、`status` 和 `access`。
- `status` 可选值为 `draft`（草稿）、`published`（已发布）、`archived`（已归档）；`access` 可选值为 `free`（免费）、`paid`（付费）。
- 发布时必须提供有效的 `published_at`。日期采用 ISO 8601 格式，时间戳使用 UTC；`updated_at` 由站长在源文件中维护。
- `tags` 为可选的人工标签；`recommended` 标记站长精选；`related` 可选填相关文章的 slug 列表；`audience` 可选填适用读者说明，始终公开。
- `preview` 仅适用于付费文章，可选 `summary`、`standard` 或 `custom`；默认使用 `standard`。选择 `custom` 时必须同时提供非空的 `preview_markdown` 字段。
- 文章可按 `<slug>/index.md` 加同目录 `assets/` 组织；Markdown 正文中只允许以相对路径引用同目录资源。
- 同步任务负责校验元数据、安全渲染 Markdown，并更新文章目录与搜索索引。任一文件无效时提供可操作的错误信息，且整次同步不发布，不覆盖最近一次有效的已发布版本。
- 同步成功后，草稿、已归档或已删除文章必须从公开列表与阅读入口移除。源文件扫描失败或不完整时，不得将其误判为批量删除。
- 修改标题或标签不能破坏文章 URL。修改已发布文章的 slug 时，必须配置明确的重定向。
- 付费文章预览仅使用 `summary` 与明确配置的试读内容，且不得将全文发送到浏览器后再隐藏正文。

### 4.3 免费用户试读机制

付费文章不对免费用户开放随机正文，而是提供站长可控的“试读卡片”，让读者判断文章是否适合自己：

- 摘要始终公开；`preview` 为 `summary` 档时仅额外展示付费提示，不提供正文试读。
- `standard` 公开正文开头约 30%（按正文纯文本字符计，中文字计 1、非中文字计 0.5），在小节或段落边界收尾，同步时生成，不在请求时截断。
- `custom` 使用 front matter 中人工维护的 `preview_markdown`，适合展示最有代表性的代码、步骤或结论。
- 试读内容必须在 Markdown 源文件中明确标记或单独存储。发布同步时生成独立的 preview 内容；服务端只向无权限用户返回 preview，不返回完整正文后再由前端截断或隐藏。
- 文章页展示“你将获得什么”信息：完整文章包含的主题、适用读者、预计阅读时长，以及试读内容之后的章节标题，但不得泄露付费正文。
- 免费用户可以查看所有文章的试读内容，但同一文章不提供多段轮换试读。按账户与 IP 对文章阅读请求限流，防止通过反复请求拼接全文。
- 试读区明确标识“试读”，在末尾放置一次主要付费行动入口；不使用遮罩、模糊或禁止复制作为保护手段。
- 试读内容可以被搜索引擎收录，但上线前需单独决定公开索引策略；付费正文仍必须禁止公开索引。

## 5. 离线打标签 Agent／定时任务

### 5.1 处理流程

1. 通过一个批处理命令手动运行，或按可配置的 cron 计划执行；初始默认每天运行一次。
2. 同步源文件，根据内容哈希与标签生成版本识别新增或变更文章。
3. 读取待处理文章，优先从受控的主题与标签词表中选择标签，可另提词表外新词建议。
4. 校验返回数据的结构与标签合法性，每篇文章最多生成八个标签。
5. 保存生成标签、源内容哈希、标签生成版本、完成时间与处理状态。
6. 标签处理成功后，刷新标签搜索与相关阅读结果。

### 5.2 标签管理与失败处理

- 人工标签优先。搜索使用“人工标签 + 有效的自动标签 − 人工拒绝的标签”。
- 站长可以通过最简审核界面或命令接受、修改或拒绝自动标签。已拒绝的标签持续保持拒绝状态，直到人工明确恢复。
- 模型提出的词表外新词落库为非公开条目，须经站长接受后才能成为公开筛选项并可被自动应用；词表内已有的有效条目可直接自动应用。单篇文章生效标签不超过八个，且必须来自词表。
- 文章无需等待打标签完成即可发布；标题搜索与人工标签应立即可用。
- 已成功处理且未变更的文章直接跳过。通过任务锁防止重复并发执行。每次运行对临时故障最多重试三次，仍失败时保留可见状态，供后续重跑。
- 标签生成失败时保留上一次成功结果；处理期间源内容发生变化时，丢弃本次过期结果。
- 文章文本作为不可信模型输入处理，不得改变任务指令、调用工具或执行代码。所有模型输出必须校验后才能保存。
- Agent 不得自行发布文章、修改价格或权限，也不得改写正文。

## 6. 读者体验

### 6.1 页面

| 页面 | 必需功能 |
| --- | --- |
| 首页／文章库 | 精选推荐、最新文章、标题搜索、主题与标签筛选 |
| 搜索结果页 | 分页文章卡片、当前筛选条件、清除／重置操作、无结果状态 |
| 文章页 | 标题、摘要、日期、主题与标签、正文或付费阅读提示、相关阅读 |
| 权限／价格页 | 通行证有效期、价格与币种、支付入口、兑换码入口 |
| 账户页 | 当前阅读权益与到期时间、购买状态、退出登录 |
| 站长审核入口 | 标签状态与修正、失败任务详情、兑换码与权限管理；首版可使用命令行工具 |

文章卡片展示标题、摘要、标签、发布日期，以及明确的免费／付费标记。必须具备基础键盘操作、可访问的表单标签、适合手机阅读的布局，以及清晰的加载与错误状态。

### 6.2 搜索

- 按文章标题进行子串匹配，去除查询首尾空白并忽略大小写。中文标题搜索不得依赖空格分词。首版不要求全文搜索或语义搜索。
- 标签与主题通过规范化的词表标识符进行精确筛选。
- 标题查询与筛选条件采用 AND 关系；同时选择多个标签时也采用 AND，即必须全部满足。界面需明确说明这一行为。
- 默认按发布时间从新到旧排序；时间相同时按 slug 稳定排序。
- 查询词、筛选条件与页码保存在 URL 中，支持收藏与分享结果。
- 仅展示已发布文章的元数据。付费文章的元数据可以被检索，但搜索结果不得返回付费正文或试读正文片段；试读内容只在文章页按规则展示。
- 限制查询长度、筛选条件数量与每页条数；服务端校验输入，数据库查询使用参数化语句。

### 6.3 推荐阅读

- 首页：优先展示站长标记为 `recommended` 的文章，再以最新发布文章补足，最多六篇。
- 文章页：优先展示站长在 `related` 中指定的有效文章，再按共同主题／标签补足至最多四篇；自动补充部分按重合数量与发布时间排序。
- 排除当前文章、重复文章、草稿与已归档文章。其他排序条件相同时按 slug 排序。
- 可以推荐付费文章，但必须明确标记；打开时仍执行正常权限校验。
- 首版推荐无需行为追踪、向量数据库或模型调用。

## 7. 支付与兑换码

### 7.1 统一权益模型

- 购买与兑换均要求登录，阅读权限绑定账户，而非仅绑定浏览器会话。
- 每次支付或兑换对应一条独立的权益授予记录，包含来源、时长、状态与审计记录。
- 新权益从“当前时间”和“现有到期时间”中较晚者开始延长。每条授予记录通过事务保证只生效一次。
- 权限到期后恢复为预览模式。每次请求受保护内容都必须校验权限；若支持下载或受保护附件，也必须执行同样的检查。
- 管理员撤销授予后，根据剩余有效授予记录重新计算权限，不得移除其他支付或兑换带来的无关权益。

### 7.2 支付流程

1. 展示一种通行证，明确价格、币种、有效期，并明确声明虚拟阅读权益一经开通不支持退款。
2. 支持微信支付、支付宝，并保留 Stripe 国际卡通道；服务端创建订单与二维码／跳转／JSAPI 支付请求，各通道统一权益。
3. 以支付服务商经过验证的服务端支付事件作为付款确认依据，不以浏览器跳转到成功页为依据。
4. 校验事件真实性，以及对应订单、预期金额、币种和支付状态。安全处理重复或乱序事件，确保权益只授予一次。
5. 展示待支付、已支付、失败与已取消状态。回调延迟或丢失时，对未完成订单进行对账。
6. 首版不提供退款流程；支付争议等异常情况由管理员人工撤销对应的权益授予记录处理，并保留审计轨迹。

不存储银行卡信息，支付服务凭据仅保留在服务端。具体支持哪些支付方式在上线前确定，本草案不作承诺。

### 7.3 兑换码流程

- 管理员使用密码学安全的随机数生成兑换码，设置阅读时长、可选的兑换截止时间，并限制为一次性使用。
- 明文仅在签发时展示，持久化保存密码学哈希与不敏感的展示标识符。
- 读者登录后兑换。有效兑换码的核销与权益授予必须原子化完成，并发请求不得重复授予权益。
- 同一账户重试兑换时返回已有结果，不再次延长权限。
- 拒绝无效、过期、已撤销或已使用的兑换码，不泄露兑换者账户。限制兑换尝试频率，日志与 URL 中不得包含兑换码明文。
- 管理员可以停用尚未使用的兑换码。撤销已授予的权限属于另一项明确操作，必须记录审计信息。
- 首版不支持共享密码、无限次复用兑换码或面向读者的公开 API Key。

## 8. 防爬取与内容保护

目标是减少自动化批量抓取，并落实付费访问控制。只要文章可在浏览器中阅读，就无法彻底阻止已授权读者复制、截图或抓取。

- 在服务端执行身份验证与权益校验。不得将受保护的 Markdown、完整正文 JSON、包含正文的 source map 或付费正文放入公开静态资源包；试读内容与完整正文分开生成和返回。
- 受保护响应不得进入共享／公共缓存；公开预览与需鉴权的正文响应分离，防止跨账户缓存泄露。
- 对文章阅读、搜索、认证与兑换接口设置可配置的请求限制，按 IP 限流，登录后同时按账户限流。触发限制时返回 `429` 与重试提示。
- 限制分页范围，不提供未经身份验证的批量导出接口。
- 按需使用 `robots.txt` 与 `noindex` 引导爬虫，但不能将其作为访问控制。公开内容是否允许搜索引擎收录，在上线前确定。
- 记录必要且已脱敏的请求与错误数据，用于识别异常访问量和调整限流。只有实际滥用情况需要时才增加边缘防护验证，并保留无障碍访问能力。
- 禁用 Markdown 中的原始 HTML，或使用持续维护的清理工具进行安全过滤；阻止可执行嵌入内容与不安全的链接协议。
- 不禁用文本选择、右键菜单或键盘快捷键；这些措施无法保护内容。

## 9. 最小实现结构

- 一个 Web 应用负责页面渲染、账户访问、搜索、支付与兑换码兑换。
- 一个关系型数据库保存文章目录、生成标签、用户、订单、权益授予、兑换码与批处理状态。优先使用数据库自带查询与索引，暂不增加独立搜索服务。
- 一个批处理命令负责源文件同步与打标签，可手动运行或由 cron 调度。除非任务量确有需要，否则不引入队列基础设施。
- 使用成熟的身份认证集成，不自行设计凭据加密机制。生产环境托管于中国大陆云（一家国内云择一），ICP 备案完成前不公网上线。
- 公开文章元数据、静态构建产物与公开媒体可经 CDN 分发缓存；CDN 属公共缓存，受保护响应（付费正文、付费媒体、账户与收银台）仅在服务端判定有权访问后返回，且不得进入 CDN 或任何共享缓存。

逻辑数据记录如下，不要求每种记录都单独建表：

| 记录 | 核心字段 |
| --- | --- |
| 文章 | Slug、源文件路径与哈希、元数据、渲染内容、发布状态 |
| 词表／标签决策 | 主题或标签标识符、文章、来源、人工接受／拒绝状态 |
| 标签处理结果 | 文章哈希、标签生成版本、标签、状态、尝试次数、错误、时间戳 |
| 用户 | 认证身份与角色 |
| 订单 | 用户、支付服务商关联标识、预期金额与币种、通行证时长、支付状态 |
| 权益授予 | 用户、唯一来源关联标识、时长、授予时间、撤销状态 |
| 兑换码 | 哈希、展示标识符、时长、兑换截止时间、状态、兑换者与兑换时间 |
| 审计事件 | 操作者、操作、目标标识符、时间戳；不保存密钥或完整支付报文 |

## 10. 验收标准

1. 有效的已发布 Markdown 文件同步后出现在文章库中；元数据格式错误时给出有效提示，且不覆盖有效内容。
2. 草稿与已归档文章不出现在公开列表中，也不能通过直接 URL 打开，付费读者同样不能访问。
3. 新增或变更文章在后台获得经过校验的标签；未变更文章被跳过，单篇失败不影响其他文章处理。
4. 人工拒绝标签的决定在重新处理后仍然保留；过期任务结果不能覆盖新版本文章的标签。
5. 中英文标题均支持部分匹配搜索；标题与标签组合筛选只返回满足条件的文章，复制 URL 后可还原筛选状态。
6. 精选推荐与相关阅读遵循指定顺序，不包含未发布文章或重复内容。
7. 访客可阅读免费正文与配置的付费试读内容，但无法通过 HTML、API、静态文件或缓存获取付费正文；试读范围不超过配置上限。
8. 经验证的支付仅授予一次对应时长的权益；伪造事件、金额不符、浏览器跳转或重复回调不能额外授予权限。
9. 同一兑换码并发兑换时只允许一个账户成功；同一账户重试不会再次延长权限。
10. 权限按时到期；管理员撤销仅移除对应授予记录所贡献的权益。
11. 高频请求触发配置的限流，日志不包含兑换码明文或支付服务密钥。
12. 上线前保留可运行的自动化检查，覆盖权限边界、支付事件幂等性、兑换并发，以及搜索与标签处理行为；在所选支付服务商的沙箱中验证支付流程。

## 11. 交付顺序

1. **内容与发现：** Markdown 校验与同步、文章库与文章页、标题／标签搜索、基于固定规则的推荐。
2. **后台打标签：** 受控词表、定时批处理、审核与修正、可查看的重试状态。
3. **访问控制：** 微信登录（保留 GitHub／邮件方式）、付费预览、权益授予、一次性兑换码、服务端内容保护与请求限流。
4. **支付与上线：** 配置微信支付、支付宝与保留的 Stripe 通道、收银台与支付事件处理及对账、沙箱验证、生产环境运维（境内云部署、备案域名与 CDN 启用）。

在访问控制完成前，付费文章的完整正文必须保持不可访问；可以先实现独立的受限试读内容，但不得把完整正文作为免费内容公开。

## 12. 相关功能实现前需确定的事项

- 首发市场与托管地区已定：面向中国大陆用户，生产环境托管于中国大陆云。是否允许搜索引擎收录公开文章仍待上线前决定。（界面语言已定为中文。）
- “Key 方式”按本文假设的兑换码推进；若实际指其他身份认证或 API Key 流程，需重新评审。
- 通行证价格、币种、有效期；是否确实需要订阅制。（首版不支持退款，不再待定。）
- 微信支付、支付宝为首版必需；明确各通道商户审核、设备场景与产品开通情况。ICP 备案为硬前提（商户入驻、回调域名、国内 CDN 加速域名均依赖，需企业主体），阶段 1 启动办理，备案完成前不公网上线。
- 微信网站应用登录配置已确定；离线打标签使用 OpenAI 兼容模型服务，允许正文出站到模型服务；若未来要求正文留在本地基础设施，打标签输入降级为标题、摘要与试读。

首版不包含：浏览器内文章编辑、读者投稿、评论、社交信息流、个性化追踪、全文／语义搜索、多商户分账与复杂订阅计费、退款流程，以及独立移动应用。
